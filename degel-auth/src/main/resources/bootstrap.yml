server:
  port: 9200

spring:
  application:
    name: degel-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  redis:
    host: localhost
    port: 6379

degel:
  security:
    jwt-secret: degel-jwt-secret-key-2024-platform-admin
